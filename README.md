# EasySong Reverse Game

Самостоятельная браузерная игра «Скажи наоборот» для привлечения аудитории в EasySong.

## Принципы

- полностью отдельный проект, без зависимостей от кода BOTYARA или EasySong;
- один web-клиент работает как обычный сайт и как Telegram Mini App;
- два режима дуэли: передача одного устройства и приватная онлайн-комната по invite URL;
- на одном устройстве игроки по очереди загадывают и повторяют перевёрнутую запись, а сходство аудио считается локально;
- онлайн загадчик вводит секретную фразу и записывает её, а соперник повторяет перевёрнутый звук, слушает свою запись в нормальном звучании и пишет догадку;
- оригинал загадчика остаётся локальным; между устройствами временно передаются только перевёрнутый challenge и попытка;
- переход в EasySong — только через внешний CTA с измеримой атрибуцией;
- production-инфраструктура планируется в отдельном контуре Yandex Cloud.

## Структура

```text
apps/web           React + TypeScript + Vite
apps/api           FastAPI backend
apps/telegram-bot  отдельный Telegram bot
packages/audio-engine  локальная подготовка, разворот и сравнение аудио
infra              заготовки инфраструктуры Yandex Cloud
```

## Быстрый старт web

```bash
npm install
npm run dev:web
```

Web-приложение по умолчанию откроется на `http://localhost:5173`.

## Локальная разработка backend

```bash
python -m venv .venv
# Windows: .venv\Scripts\activate
# Linux/macOS: source .venv/bin/activate
pip install -r apps/api/requirements.txt
uvicorn apps.api.app.main:app --reload --port 8000
```

## Режим на одном устройстве

1. Игрок 1 записывает слово или короткую фразу и проверяет перевёрнутый звук.
2. Устройство передаётся Игроку 2: он слушает запись наоборот и повторяет услышанное.
3. Клиент разворачивает попытку обратно и локально сравнивает её с оригиналом.
4. Во втором раунде игроки меняются ролями. После результата можно прослушать локальные записи или выйти в стартовое меню.

Ни оригиналы, ни попытки локальной дуэли не покидают устройство и очищаются при выходе из игры.

## Cross-device протокол и безопасность

1. Загадчик отправляет секретную фразу в API. До ответа она возвращается только самому загадчику и не раскрывается сопернику.
2. Загадчик записывает фразу, локально переворачивает её и загружает только WAV с перевёрнутым звуком.
3. Соперник скачивает клип по короткоживущему signed GET, повторяет услышанные звуки и временно загружает попытку.
4. Клиент соперника разворачивает его попытку обратно, даёт прослушать нормальное звучание и предлагает написать догадку.
5. API нормализует фразу и догадку, рассчитывает текстовое сходство, раскрывает результат обоим игрокам и удаляет оба аудиообъекта. Bucket имеет lifecycle-правило удаления объектов с префиксом `matches/` максимум через сутки как аварийную страховку; подписанные URL и metadata истекают через `AUDIO_TTL_SECONDS` (по умолчанию 20 минут).

Object Storage обязан быть приватным: без website hosting и public ACL. Для runtime задаются `S3_BUCKET`, `S3_ACCESS_KEY_ID`, `S3_SECRET_ACCESS_KEY`. PostgreSQL-миграции из `apps/api/migrations` применяются при старте API под advisory lock и записываются в `schema_migrations`; их также можно последовательно применить вручную. `create_all` остаётся удобством для пустого стенда.

Cross-device матч использует backend как единственный источник игрового состояния. Клиенты опрашивают state раз в секунду и применяют versioned snapshots по `revision`; heartbeat идёт раз в 5 секунд. Participant token хранится локально и позволяет занять прежний slot после refresh/reconnect. Activity statuses влияют только на live UX и не могут выполнять игровые переходы. Оригинальная запись загадчика живёт только в памяти браузера и никогда не отправляется в API.

Telegram-бот развёрнут отдельно на Railway в режиме long polling. Дуэльный релиз не меняет его runtime и не возвращает webhook/function в Yandex Cloud; сайт, API, PostgreSQL и временный bucket остаются в Yandex Cloud.

## Безопасность

Никогда не коммитить `.env`, токены Telegram, ключи Yandex Cloud или доступы к PostgreSQL. Для production секреты будут храниться в Yandex Lockbox.
